{
  "id": "todo-list",
  "name": "To-Do List",
  "html": "<div class=\"widget-card\">\n  <div class=\"widget-header\">\n    <h4 class=\"widget-title\">📝 Tasks</h4>\n    <div class=\"progress-circle\" id=\"progress-text\">0%</div>\n  </div>\n\n  <div class=\"input-group\">\n    <input type=\"text\" id=\"todo-input\" placeholder=\"What needs to be done?\">\n    <button onclick=\"addTodo()\">+</button>\n  </div>\n\n  <div class=\"todo-list\" id=\"todo-list\"></div>\n  \n  <div class=\"footer\">\n    <span id=\"items-left\">0 items left</span>\n    <button class=\"btn-clear\" onclick=\"clearCompleted()\">Clear Completed</button>\n  </div>\n</div>",
  "css": ":host { display: block; margin: 1.5rem 0; font-family: var(--font-interface); }\n.widget-card {\n    background: var(--background-secondary);\n    border: 1px solid var(--divider-color);\n    border-radius: 16px;\n    padding: 1.5rem;\n    box-shadow: 0 4px 20px rgba(0,0,0,0.06);\n}\n\n.widget-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; }\n.widget-title { margin: 0; font-size: 1.1rem; font-weight: 800; color: var(--text-normal); }\n\n.progress-circle {\n    width: 40px;\n    height: 40px;\n    border-radius: 50%;\n    background: var(--interactive-accent);\n    color: white;\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    font-size: 0.75rem;\n    font-weight: 800;\n}\n\n.input-group {\n    display: flex;\n    gap: 10px;\n    margin-bottom: 1.5rem;\n}\n\n.input-group input {\n    flex: 1;\n    background: var(--background-primary);\n    border: 1px solid var(--divider-color);\n    color: var(--text-normal);\n    border-radius: 10px;\n    padding: 10px 14px;\n    font-size: 0.9rem;\n    outline: none;\n}\n.input-group input:focus { border-color: var(--interactive-accent); }\n\n.input-group button {\n    background: var(--interactive-accent);\n    color: white;\n    border: none;\n    width: 40px;\n    height: 40px;\n    border-radius: 10px;\n    font-size: 1.5rem;\n    cursor: pointer;\n}\n\n.todo-list {\n    display: grid;\n    gap: 8px;\n    margin-bottom: 1.5rem;\n    max-height: 300px;\n    overflow-y: auto;\n}\n\n.todo-item {\n    display: flex;\n    align-items: center;\n    gap: 12px;\n    padding: 12px 16px;\n    background: var(--background-primary-alt);\n    border-radius: 12px;\n    border: 1px solid var(--divider-color);\n    transition: all 0.2s;\n}\n.todo-item.done { opacity: 0.6; }\n.todo-item.done .text { text-decoration: line-through; }\n\n.checkbox {\n    width: 20px;\n    height: 20px;\n    border: 2px solid var(--interactive-accent);\n    border-radius: 6px;\n    cursor: pointer;\n    display: flex;\n    align-items: center;\n    justify-content: center;\n}\n.checkbox.checked { background: var(--interactive-accent); }\n.checkbox.checked::after { content: \"✓\"; color: white; font-size: 12px; font-weight: 800; }\n\n.text { flex: 1; font-size: 0.9rem; font-weight: 600; color: var(--text-normal); }\n\n.btn-del { background: none; border: none; color: var(--text-error); cursor: pointer; font-size: 0.8rem; opacity: 0; transition: opacity 0.2s; }\n.todo-item:hover .btn-del { opacity: 0.6; }\n.btn-del:hover { opacity: 1 !important; }\n\n.footer {\n    display: flex;\n    justify-content: space-between;\n    align-items: center;\n    font-size: 0.75rem;\n    color: var(--text-muted);\n    font-weight: 600;\n}\n\n.btn-clear { background: none; border: none; color: var(--text-muted); cursor: pointer; text-decoration: underline; }\n.btn-clear:hover { color: var(--text-error); }",
  "js": "const { root, saveState, getState } = api;\n\nlet todos = [];\n\nasync function init() {\n    const saved = await getState();\n    if (saved) todos = saved;\n    render();\n}\n\nfunction render() {\n    const list = root.getElementById('todo-list');\n    list.innerHTML = '';\n    \n    let doneCount = 0;\n    todos.forEach((todo, idx) => {\n        if (todo.done) doneCount++;\n        const item = document.createElement('div');\n        item.className = `todo-item ${todo.done ? 'done' : ''}`;\n        item.innerHTML = `\n            <div class=\"checkbox ${todo.done ? 'checked' : ''}\" onclick=\"toggleTodo(${idx})\"></div>\n            <span class=\"text\">${todo.text}</span>\n            <button class=\"btn-del\" onclick=\"deleteTodo(${idx})\">Delete</button>\n        `;\n        list.appendChild(item);\n    });\n    \n    const progress = todos.length > 0 ? Math.round((doneCount / todos.length) * 100) : 0;\n    root.getElementById('progress-text').innerText = `${progress}%`;\n    root.getElementById('items-left').innerText = `${todos.length - doneCount} items left`;\n}\n\naddTodo = async () => {\n    const input = root.getElementById('todo-input');\n    const text = input.value.trim();\n    if (text) {\n        todos.push({ text, done: false });\n        input.value = '';\n        render();\n        await saveState(todos);\n    }\n};\n\ntoggleTodo = async (idx) => {\n    todos[idx].done = !todos[idx].done;\n    render();\n    await saveState(todos);\n};\n\ndeleteTodo = async (idx) => {\n    todos.splice(idx, 1);\n    render();\n    await saveState(todos);\n};\n\nclearCompleted = async () => {\n    todos = todos.filter(t => !t.done);\n    render();\n    await saveState(todos);\n};\n\ninit();",
  "tags": [
    "productivity",
    "tasks",
    "organization"
  ]
}